"""Ensemble forecast combining NOAA + Open-Meteo models."""

# Phase 2: Combine NOAA + Open-Meteo (GFS, ECMWF, ICON) with weights
# Return (mean_temp, std_dev) where std_dev grows with forecast horizon
